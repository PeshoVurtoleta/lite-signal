#!/bin/bash
# run-all-benchreactive.sh -- RETIRED (bench protocol v3).
# Drove benchmarkReactive.mjs (now a retired stub). The reactive field runs unified:
#   node --expose-gc bench/sweep.mjs         # 47 rows, per-row cold isolation
echo "run-all-benchreactive.sh is RETIRED. Use: node --expose-gc bench/sweep.mjs" >&2
exit 2
